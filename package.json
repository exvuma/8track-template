{
  "private": true,
  "version": "1.0.0",
  "description": "A template for starting a Cloudflare Worker project with 8track",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "dependencies": {
    "8track": "^1.0.7",
    "prettier": "^1.18.2"
  }
}
